
export enum COMMISSION_STATUS {
  paid = 'paid',
  cancel = 'cancelled',
  forControl = 'for_control',
  notForControl = 'not_for_control'
}

export const commissionStatus = (status) => {
  // let
}
