import React from 'react';
import { View, StyleSheet } from 'react-native';

interface Props{}

const RegisterLoan = React.memo((props: Props) => {
  return (
    <View style={styles.container}>
    
    </View>
  )
});

export default RegisterLoan;

const styles = StyleSheet.create({
    container: {},
});
