import React from 'react';
import { View, StyleSheet } from 'react-native';

interface Props{}

const InvestScreen = React.memo((props: Props) => {
  return (
    <View style={styles.container}>

    </View>
  )
});

export default InvestScreen;

const styles = StyleSheet.create({
    container: {},
});
